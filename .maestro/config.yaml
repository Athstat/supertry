# Maestro Configuration for Scrummy App
# This file contains environment variables and app configuration

appId: com.scrummy.mobile

env:
  # Base URL for the web app (adjust for your environment)
  BASE_URL: "http://localhost:5173"
  
  # API endpoints (if needed for setup/teardown)
  API_URL: "your-api-url"
  
  # Test user credentials
  TEST_EMAIL: "maestro_test@example.com"
  TEST_PASSWORD: "TestPassword123!"
  TEST_USERNAME: "maestro_test_user"
  
  # Timeouts
  MAESTRO_DRIVER_STARTUP_TIMEOUT: 120000
  
  # Test data
  LEAGUE_ID: "1"
  TEAM_NAME: "Maestro Test Team"
