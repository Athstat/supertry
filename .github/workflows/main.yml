name: Codex pull request review

on:
  pull_request:
    types: [opened, synchronize, reopened]
  issue_comment:
    types: [created]

jobs:
  codex:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
    # Combined if conditions - only run on PRs AND when summoned for comments
    if: ${{ (github.event_name == 'pull_request') || (github.event_name == 'issue_comment' && github.event.issue.pull_request && (contains(github.event.comment.body, '@CoachScrummy') || contains(github.event.comment.body, '@coachscrummy') || contains(github.event.comment.body, '@COACHSCRUMMY'))) }}
    outputs:
      final_message: ${{ steps.run_codex.outputs.final-message }}

    steps:
      # Get PR number from different event types
      - name: Get PR number
        id: pr_number
        run: |
          if [ "${{ github.event_name }}" = "issue_comment" ]; then
            # For comment events, use issue.number (PRs are also issues)
            echo "pr_number=${{ github.event.issue.number }}" >> $GITHUB_OUTPUT
          else
            # For PR events, use the direct PR number
            echo "pr_number=${{ github.event.pull_request.number }}" >> $GITHUB_OUTPUT
          fi

      - uses: actions/checkout@v5
        with:
          ref: refs/pull/${{ steps.pr_number.outputs.pr_number }}/merge
          fetch-depth: 0

      # Resolve base ref for both PR and comment events
      - name: Resolve base ref
        id: pr_meta
        run: |
          if [ "${{ github.event_name }}" = "issue_comment" ]; then
            # Install GitHub CLI and get base ref for comment events
            curl -fsSL https://cli.github.com/packages/githubcli-archive-keyring.gpg | sudo dd of=/usr/share/keyrings/githubcli-archive-keyring.gpg
            echo "deb [arch=$(dpkg --print-architecture) signed-by=/usr/share/keyrings/githubcli-archive-keyring.gpg] https://cli.github.com/packages stable main" | sudo tee /etc/apt/sources.list.d/github-cli.list > /dev/null
            sudo apt update
            sudo apt install gh -y
            
            echo "base_ref=$(gh pr view ${{ steps.pr_number.outputs.pr_number }} --json baseRefName -q .baseRefName)" >> "$GITHUB_OUTPUT"
          else
            echo "base_ref=${{ github.event.pull_request.base.ref }}" >> "$GITHUB_OUTPUT"
          fi

      # Fetch base branch for comparison
      - name: Fetch base branch
        run: |
          git fetch origin ${{ steps.pr_meta.outputs.base_ref }}

      # Get list of changed files
      - name: Get changed files
        id: changed_files
        run: |
          FILES=$(git diff --name-only origin/${{ steps.pr_meta.outputs.base_ref }}...HEAD)

          echo "Changed files:"
          echo "$FILES"

          {
            echo "files<<EOF"
            echo "$FILES"
            echo "EOF"
          } >> $GITHUB_OUTPUT

      # Run Codex only on changed files
      - name: Run Codex
        id: run_codex
        uses: openai/codex-action@v1
        with:
          openai-api-key: ${{ secrets.CODEX_API_KEY }}
          prompt-file: .github/codex/prompts/review.md
          output-file: codex-output.md
          safety-strategy: drop-sudo
          sandbox: workspace-write
        env:
          CHANGED_FILES: ${{ steps.changed_files.outputs.files }}
          COMMENT_BODY: ${{ github.event.comment.body || '' }}

  post_feedback:
    runs-on: ubuntu-latest
    needs: codex
    if: needs.codex.outputs.final_message != '' && (
          github.event_name != 'issue_comment' || 
          contains(github.event.comment.body, '@CoachScrummy') ||
          contains(github.event.comment.body, '@coachscrummy') ||
          contains(github.event.comment.body, '@COACHSCRUMMY')
        )
    steps:
      - name: Post Codex feedback
        uses: actions/github-script@v7
        with:
          github-token: ${{ github.token }}
          script: |
            const message = `**🏈🔥 Coach Scrummy's Code Review:**\n\n${process.env.CODEX_FINAL_MESSAGE}`;

            if (context.eventName === 'issue_comment' && context.payload.comment) {
              // For issue comments, create a regular comment (no in_reply_to)
              await github.rest.issues.createComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: context.payload.issue.number,
                body: message
              });
            } else {
              // New PR or PR update
              await github.rest.issues.createComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: context.payload.pull_request.number,
                body: message
              });
            }
        env:
          CODEX_FINAL_MESSAGE: ${{ needs.codex.outputs.final_message }}