export type RadioListOption = {
    label: string,
    value: string
}

export type DropdownOption = {
    label: string,
    value: string
}

export type TooltipData = {
    title?: string,
    description?: string
}