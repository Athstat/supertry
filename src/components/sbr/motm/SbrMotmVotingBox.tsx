import { ISbrFixture } from "../../../types/sbr"

type Props = {
    fixture: ISbrFixture
}

export default function SbrMotmVotingBox({fixture} : Props) {
  return (
    <div>SbrMotmVotingBox</div>
  )
}
